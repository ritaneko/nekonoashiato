<% content_for(:title, '一覧ページ') %>

<div class="container">
  <div class="row">
    <div class="col-mb-3">
      <% @tag_list.each do |list| %>
       <i class="fa-sharp fa-solid fa-tag"></i>
        <%= link_to list.tag_name, search_path(tag_id: list.id) %>
        <%= "(#{list.photos.count})" %>
      <% end %>
         <div class="card bg-dark text-white">
           <% @photos.each do |photo| %>

           <%= link_to photo_path(photo.id) do %>
            <%= image_tag photo.get_image, class: "card-img-top"; %>
           <% end %>
             <div class="card-body">
                <div class="row">
                  <div class="col-2">
                    <%= image_tag photo.user.get_profile_image(100,100), class: "mt-3 rounded-circle"; %>
             </div>
             <div class="col-10">
                <p class="card-title">
                  <%= link_to(photo_path(photo.id), class: "text-dark") do %>
                     <strong><i class="fa-solid fa-paw-simple"></i><span><%= photo.title %></span></strong>
                  <% end %>
               </p>
               <p class="card-text">一言：<%= photo.body %></p>
                 <span><i class="fas fa-user"></i> by <%= photo.user.name %> ｜ </span>
                 <span><%= link_to "#{photo.photo_comments.count} コメント", photo_path(photo.id), class: "text-dark" %></span>
                   <p>タグ:#<%= link_to tag.tag_name, photos_path(tag_id: tag.id) %></p>
             </div>
           </div>
      </div>
   </div>
</div>
    <% end %>
  <%= paginate @photos %>