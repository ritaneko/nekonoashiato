<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h4 class="headline mt-5 ml-5">いいねした投稿</h4>
        <div class="row mt-3">
        <% @liked_photos.each do |photo| %>
         <%= render 'public/favorites', photo: photo %>
        <% end %>
      </div>
      <div class="d-flex justify-content-center">
        <%= paginate @liked_photos, theme: 'bootstrap-5' %>
      </div>
    </div>
  </div>
</div>