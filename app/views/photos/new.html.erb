<h1>フォト投稿</h1>
<%= form_with model: @photo do |f| %>

 <h4>タイトル</h4>
 <%= f.text_field :title %>
 <h4>画像</h4>
 <%= f.file_field :image, accept: "image/*" %>
 <h4>コメント</h4>
 <%= f.text_area :body %>
 <%= f.submit '投稿' %>
<% end %>
